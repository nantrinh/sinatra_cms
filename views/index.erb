<% if session[:error] %>
  <p><%=session.delete(:error)%></p>
<% end %>

<ul>
  <% @filenames.each do |filename| %>
    <li>
      <a href="<%=filename%>"><%=filename%></a>
    </li>
  <% end %>
</ul>
